stage: synthesis.population.trips.final
title: Reporting-day trips
layer: behavior
description: 'Reporting-day view of the population''s day (ADR-0104, issue #244): the
  pre-assignment synthesis.population.trips with the day of every worker drawn to
  ''home'' replaced by a matched MiD home-office donor''s day and the trips of every
  worker drawn to ''absent'' removed. The pre-assignment view itself stays untouched and
  keeps feeding the commute distances and the primary location assignment, which is what
  keeps the day state -- it depends on the assigned distance -- out of a dependency cycle
  with the location assignment. Everything that needs the FINISHED day reads this stage:
  the secondary chainsolvers, synthesis.population.activities.final, synthesis.output, the
  MATSim population and the two commute analyses. With commute_day_state_enabled false the
  stage returns the pre-assignment frame itself, so the alias is a pure pass-through.'
lineage:
  type: braunschweig_new
  upstream: null
  notes: No eqasim precedent; a second, terminal view of the trips table introduced by
    ADR-0104. Only the configs/base_bs.yml production config resolves it to the model; every
    configs/fixtures/* config aliases it to its own pre-assignment trips stage instead.
code:
- braunschweig/synthesis/commute_day/trips_day_stage.py
- braunschweig/synthesis/commute_day/plan_replacement.py
pipelines:
  popsim_mid: active
  popsim_open: not_used
  simple_ipf_open: not_used
production: true
resolves_to:
  popsim_mid: braunschweig.synthesis.commute_day.trips_day_stage
inputs:
- mid2023_home_office_day_donors
- srv2023_departure_time_reference
features:
- commute_day_state
- general_day_absence
- departure_time_model
decisions:
- ADR-0104
- ADR-0110
- ADR-0114
notes: 'The replacement logic itself lives entirely in plan_replacement.build_day_trips (a pure
  module, no synpp/no I/O): per person, by commute_day_state -- absent gets NO rows at all; home
  WITH a donor match gets every original row dropped and replaced by the donor''s own trip chain
  (person_id rewritten to the RECEIVING person, trip_index renumbered 0..n-1 in the donor''s own
  order, is_first_trip/is_last_trip recomputed; trip_key is copied VERBATIM from the donor and
  therefore still names the donor and is not unique once a donor is reused -- useful only for
  tracing a row back to its donor trip, never as a per-row identifier; any input column beyond the
  CONTRACT plus euclidean_distance/trip_key is set to NaN on replaced rows rather than inventing a
  donor-side value); home WITHOUT a donor match keeps the person''s ORIGINAL rows unchanged
  (state_stage is expected to have already downgraded these to at_workplace -- ADR-0104''s
  documented default -- so an unmatched home person reaching this stage is itself a signal, not
  the normal path); at_workplace, or any person absent from the states frame altogether, keeps
  their rows unchanged. The per-person departure-time jitter
  (braunschweig.popsim.trips_stage.apply_per_person_jitter) is applied EXACTLY ONCE here, across
  all replaced rows together, keyed by the RECEIVING person_id -- the donor pool deliberately omits
  it so the same donor day is never jittered twice.

  Guards and fallback rates, all logged: n_home_unmatched (home persons with no donor match still
  reaching this stage, kept unchanged and warned about -- see above) and
  n_donors_without_trips / share_donors_without_trips (matched persons whose donor_id has ZERO
  rows in donor_trips -- legitimately a fully immobile home-office day in the common case, but ALSO
  exactly the symptom a donor_id key/dtype mismatch between matches and donor_trips would produce
  for EVERY replaced person; warned above 1% for that reason, CLAUDE.md fallback transparency).
  Diagnostics: n_persons_replaced, n_persons_absent, n_trips_removed, n_trips_added,
  n_home_unmatched, n_extra_columns_nulled, n_donors_without_trips -- logged by the stage on every
  execute().

  The cache token (validate()) is an md5 of plan_replacement''s source alone (synpp hashes only
  trips_day_stage.py itself, so an edit to the pure replacement module would otherwise leave a
  stale cached view in place) -- the same mechanism as
  braunschweig.synthesis.locations.secondary_chainsolvers.validate. On the OFF path execute()
  returns the pre-assignment trips frame BY IDENTITY (the same object, not a copy), so the alias is
  byte-identical by construction, not merely by equal values.

  popsim_open and simple_ipf_open declare commute_day_state_enabled: false in their committed
  fixtures (configs/fixtures/config_popsim_open_braunschweig.yml,
  configs/fixtures/config_local_braunschweig_25pct.yml); their synpp DAGs never reach
  trips_day_stage at all -- not_used there is the DAG''s own statement, not merely an unexercised
  default. Evidence: tests/test_commute_day_plan_replacement.py (the pure replacement rules,
  primary-method coverage of each commute_day_state branch) and tests/test_commute_day_stages.py
  (test_trips_day_stage_off_returns_the_identical_object, test_trips_day_stage_on_replaces_only_the_home_persons,
  test_matches_from_states_keeps_only_home_persons_with_a_donor).

  General day absence (issue #370, ADR-0110): the general day-absence draw is INDEPENDENT of the
  commute-day-state model above, gated by its own config key day_absence_enabled
  (trips_day_stage.KEY_DAY_ABSENCE_ENABLED, same default true) and read from the new stage
  braunschweig.synthesis.day_absence.absence_stage
  (docs/registry/stages/braunschweig.synthesis.day_absence.absence_stage.yml). Composition rule,
  seed and diagnostics all live in plan_replacement.build_day_trips''s general_absence keyword,
  documented once in the feature record general_day_absence and the contributor note
  docs/codebase/notes/day-absence-state.md -- not repeated here. trips_day_stage.configure()
  declares braunschweig.synthesis.day_absence.absence_stage UNCONDITIONALLY, the same
  always-declared-dependency trade-off state_stage/home_office_donors_stage already made for
  commute_day_state_enabled -- a STABLE declaration list, this stage''s own local convention
  (corrected wording, final-review fix wave, Important finding 7): state_stage and
  home_office_donors_stage were already declared unconditionally before this feature existed,
  and the absence stage''s OFF path is equally trivial, not a limitation of the test-harness stub
  context (tests/test_commute_day_stages.py''s recorders were extended with config overrides by
  Tasks 6/7 of the #370 SDD plan, so that is no longer an accurate justification). Sibling
  consumers of the same stages (plan_structure_vs_srv, work_participation_by_kreis, output_day,
  the MATSim population wrapper) DO gate their own declarations on the flag in configure(), which
  stays equally legitimate; the gating here happens entirely in execute() instead. With BOTH
  flags false the stage still returns the pre-assignment frame BY IDENTITY,
  covered by the dedicated identity test
  tests/test_commute_day_stages.py::test_trips_day_stage_off_off_returns_the_identical_object.
  configs/fixtures/config_popsim_open_braunschweig.yml sets day_absence_enabled: false (ruling
  R13 of the #370 SDD plan, correcting ruling R2''s wrong premise that no
  synthesis.population.trips.final alias existed there at all): the alias DOES exist there, to
  the bare pre-assignment stage braunschweig.popsim.trips_stage (not day-absence-aware), but
  matsim.scenario.population is ALSO aliased there, to the braunschweig wrapper
  (docs/registry/stages/matsim.scenario.population.yml), which gates its OWN declaration/read of
  braunschweig.synthesis.day_absence.absence_stage on this SAME flag, independent of the
  trips.final alias. Leaving the flag at its true default would therefore make the wrapper merge
  a dayAbsenceState attribute onto every resident person while their trips remain untouched (the
  same inconsistency the final-review fix wave''s Important finding 1 closed for
  configs/fixtures/config_popsim_mid_braunschweig.yml) -- false is NECESSARY here, not merely set
  for parity with commute_day_state_enabled. configs/fixtures/config_smoke_popsim_open_mini.yml
  also sets day_absence_enabled: false, but for the ORIGINAL parity reason: it aliases neither
  matsim.scenario.population nor synthesis.output to a day-absence-aware stage (its run: list
  does not even include matsim.output), so the flag genuinely has no consumer there.
  configs/fixtures/config_local_braunschweig_25pct.yml (simple_ipf_open) leaves
  day_absence_enabled at its code default for the identical no-consumer reason: it aliases
  neither matsim.scenario.population nor synthesis.population.trips.final to anything
  day-absence-aware (the .final alias there is the bare pre-assignment stage
  synthesis.population.trips), so the flag remains unreachable there regardless of its value.


  DEPARTURE-TIME MODEL, SECOND CALL SITE (issues #123 / #384, ADR-0114, feature record
  departure_time_model). The spliced rows of this stage are the ONLY place besides
  braunschweig.popsim.trips_stage.run where a departure time is shifted, because the home-office
  donor pool deliberately leaves its days un-jittered so a donor day is never shifted twice
  (ADR-0104). trips_day_stage therefore declares the SAME four keys as the pre-assignment stage
  (departure_time_model, departure_time_mapping_min_reference_n, departure_time_mapping_min_model_n,
  departure_time_mapping_max_median_shift_hours, all from braunschweig.popsim.stage.config_keys,
  plus data_path) and passes them, with the loaded reference, into
  plan_replacement.build_day_trips as a frozen DepartureTimeSettings; the reference comes from
  the SAME helper both stages use (departure_time_model.load_reference_for_model), so a missing
  file fails identically here and there, naming the path and the key. No new stage dependency was
  needed: the harmonised group is built by persons_from_synthetic_schema from the
  synthesis.population.enriched frame this stage already reads (ruling A-R13 of the #123 SDD
  plan). The mapping cell is keyed by the RECEIVING person group and the DONOR chain first
  purpose, and the persons handed to the model are RESTRICTED to the replaced set, because the
  quantile mapping ranks a cell persons against each other -- passing the whole population would
  give a replaced person a quantile computed against people this call is not touching.
  departure_time=None keeps apply_per_person_jitter byte-identically, and OFFSET_COLUMN stays in
  _RECOMPUTED_COLUMNS so it is neither nulled nor counted as an extra column on replaced rows
  (the model overwrites it a few lines later). The model diagnostics are returned under
  diagnostics["departure_time"] (None on the jitter path and when nothing was replaced -- never a
  substituted empty report) and the stage logs the model name plus the coarsening-level split via
  the shared departure_time_model.format_level_split. _HELPER_MODULES gained departure_time_model,
  calibration.reported_time_precision, calibration.srv_departure_times,
  calibration.srv_plan_structure (it supplies the group rule the mapping cell is keyed on) and
  popsim.plan_validation (its MAX_PLAN_TIME_SECONDS is the upper clip bound), all five
  CROSS-package and therefore invisible to tests/test_synpp_helper_hash_invariant.py, which
  polices own-package siblings only. Known limitation, documented in ADR-0114 rather than
  repeated here: departure_time_mapping_min_model_n governs BOTH call sites, so at small sampling
  rates the much smaller spliced set coarsens or stays unmapped -- read this stage own level-split
  log line before trusting a srv_mapped reporting day. The mechanism itself lives once in
  docs/codebase/notes/departure-time-model.md.'
