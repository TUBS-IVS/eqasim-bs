feature: gravity_od
title: Gravity OD (work/edu)
area: work
description: Work/education OD distribution by a distance-decay gravity model IPF-calibrated to
  BA Pendleratlas Kreis-pair flows (slope -0.065, Poisson-GLM MLE).
stages:
- data.od.weighted
pipelines:
  popsim_mid: active
  popsim_open: active
  simple_ipf_open: active
lifecycle: active
production:
  enabled: true
  flags: []
introduced:
  issue: null
  pr: null
  adr: null
detail_doc: docs/features/gravity.md
code_paths:
- braunschweig/gravity/model.py
evidence:
  tests:
  - tests/test_gravity_model_core.py
  - tests/test_gravity_slope_config.py
  - tests/test_gravity_uses_census_alias.py
  - tests/test_gravity_ring_calibration.py
  off_path_byte_identical:
    claimed: not_applicable
    note: |
      gravity_slope is a numeric parameter (-0.065), not a boolean flag; this is baseline always-on behaviour with no OFF path.
  fallback_rate:
    instrumented: false
    note: |
      No fallback logic applies to the flat baseline slope itself (fallback logic exists only for the per-RS7 variant, see gravity_slope_by_rs7.yml).
  reference:
    kind: none
    note: |
      BA Pendleratlas (statistik_pendler_2026042493412.csv / ...430.csv under eqasim-data/data/braunschweig/) is not committed and absent from disk (local-only raw data).
validation:
  state: unvalidated
  runs: []
  metric: null
  note: No scalar KPI currently attributed to a recorded run.
assessment:
  status: pending
  by: null
  date: null
  pending_reason: |
    No ADR was found for this row specifically -- gravity_slope=-0.065 is inherited baseline functionality from the eqasim-bavaria fork point (ADR-0000/ADR-0001 record it only as a consequence, not a decision about the gravity mechanism). No owner verdict recorded.
  source: null
