feature: building_potentials_education
title: Building potentials — education
area: education
description: Distributes education-facility capacity onto the facility's own buildings via the
  building-activity-potential weights, so within-facility placement follows real building mass
  (ADR-0025).
stages:
- braunschweig.data.schools.kita_facilities
- braunschweig.data.schools.university_facilities
- synthesis.locations.education
pipelines:
  popsim_mid: active
  popsim_open: active
  simple_ipf_open: not_used
lifecycle: active
production:
  enabled: true
  flags:
  - education_building_distribution
introduced:
  issue: null
  pr: null
  adr: ADR-0025
detail_doc: docs/features/building-potentials.md
code_paths:
- braunschweig/data/schools/kita_facilities.py
- braunschweig/data/schools/university_facilities.py
evidence:
  tests:
  - tests/test_education_building_distribution.py
  off_path_byte_identical:
    claimed: not_applicable
    note: No dedicated byte-identity test name found.
  fallback_rate:
    instrumented: false
    note: No fallback logging found in either file (grep for "fallback" returned no matches).
  reference:
    kind: none
    note: |
      Per ADR-0025 and the matrix's own "within-facility" source hint, education potential is derived from the facility's own attached building-potential attribute, not from an independent external reference file -- this is the expected/correct state, not a gap.
validation:
  state: unvalidated
  runs: []
  metric: null
  note: No scalar KPI currently attributed to a recorded run.
assessment:
  status: pending
  by: null
  date: null
  pending_reason: |
    ADR-0025 documents the design; no owner verdict on realised education-potential distribution has been recorded.
  source: ADR-0025
