feature: eqasim_java_fork
title: Own eqasim-java-bs fork (2.3.0, matsim.output e2e-green 2026-07-23 at 2.2.0)
area: infrastructure
description: The MATSim/Java side builds from the project's own editable eqasim-java-bs fork (eqasim_source_path)
  rather than a released upstream jar (ADR-0038).
stages:
- matsim.runtime.eqasim
pipelines:
  popsim_mid: active
  popsim_open: active
  simple_ipf_open: active
lifecycle: active
production:
  enabled: true
  flags:
  - eqasim_source_path
introduced:
  issue: null
  pr: null
  adr: ADR-0038
detail_doc: null
code_paths:
- matsim/runtime/eqasim.py
- braunschweig/provenance.py
evidence:
  tests: []
  off_path_byte_identical:
    claimed: not_applicable
    note: There is no OFF path; the fork is the only supported eqasim-java source.
  fallback_rate:
    instrumented: false
    note: |
      No fallback/primary logging pattern exists; matsim/runtime/eqasim.py raises RuntimeError on a missing or misbuilt path rather than falling back.
  reference:
    kind: none
    note: Infrastructure feature (build/runtime wiring); no external reference applies.
validation:
  state: unvalidated
  runs:
  - matsim-e2e-2.2.0-kreis03101-2026-07-23
  metric: null
  note: |
    eqasim-java 2.2.0 matsim.output e2e wiring PROOF at 1-Kreis scale with freight (ADR-0071)
    -- a green pipeline, not a validation.
    The fork moved to v2.3.0 on 2026-08-17 (TUBS-IVS/eqasim-java-bs PR #17), its FIRST own
    release: no release had ever been cut in the fork, so that changelog collects the whole
    fork history and the tag only LABELS code the pipeline already built from source via
    eqasim_source_path. eqasim_version in matsim/runtime/eqasim.py moved with it because the
    module version drives the jar name (braunschweig-<version>.jar); nothing about the built
    code changed with the bump itself. The e2e proof above predates the release and does not
    cover the commits added to the fork since 2026-07-23.
assessment:
  status: pending
  by: null
  date: null
  pending_reason: |
    Two ADRs apply: ADR-0038 (the fork-existence decision, MATSim 2025.0-PR3568 pin) and ADR-0071 (the 2.2.0 / matsim.output e2e-green 2026-07-23 upgrade, matching the row's own date). No owner verdict beyond the ADRs' own measured 8/8 green run has been recorded here.
  source: ADR-0071
