feature: tier3_kreis_controls
title: Tier-3 Kreis controls
status: "on"
flag: braunschweig.population.popsim.control_tiers
status_matrix_row: "**[Attrs]** Tier-3 Kreis controls"
adr: ADR-0015
code_paths:
  - braunschweig/popsim/control_spec.py

evidence:
  tests:
    - tests/test_popsim_tier3_controls.py
    - tests/test_popsim_validation_tier3.py
    - tests/test_control_spec_catalog.py
  off_path_byte_identical:
    claimed: "not_applicable"
    note: control_tiers is a list-valued config key (tier0..tier3), not a boolean toggle.
  fallback_rate:
    instrumented: false
    note: >
      No rate-style fallback log found; missing kreis_controls data raises FileNotFoundError/
      ValueError (fail-fast), not a silent fallback with a rate to log.
  reference:
    kind: committed
    path: eqasim-data/data/braunschweig/popsim/zensus2022_employment_by_age_ref.csv
    note: >
      The Tier-3/employment-grid kreis_controls directory itself
      (braunschweig.population.popsim.kreis_controls_dir) is explicitly gitignored and absent
      from this checkout; only this companion employment-by-age reference file is committed.
  kpi:
    name: null
    produced_by: null
    last_value: null
    measured_on: null
    note: No scalar KPI currently attributed to a recorded run.

assessment:
  status: pending
  by: null
  date: null
  pending_reason: >
    ADR-0015 documents the design (Codeplan-B1 crosswalk). docs/features/regional-control-targets.md
    exists but documents a different (5-entry economic_status/cars/bicycles/ebike/trip_class)
    registry, not this Tier-3 mechanism -- the detail_doc does not actually cover this feature,
    so it is omitted from this declaration rather than pointed at the wrong doc. No owner
    verdict on realised Tier-3 fit has been recorded.
  source: ADR-0015
